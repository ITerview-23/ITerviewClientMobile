import React from 'react';
import {Text, View, SafeAreaView, Modal} from 'react-native';

function QuizAnswerModal({visible, onClose}): JSX.Element {
  return (
    <Modal>
      <Text>Hi</Text>
    </Modal>
  );
}

export default QuizAnswerModal;
